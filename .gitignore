venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.git/
logs/
test_config.yaml
test_models.py
*.tar.gz
build/
dist/
*.spec
.backups/
pip-cache/
dist_windows/

# Secrets & state (避免 push protection 拦截)
config.local.yaml
**/keys.json
**/key_vault/
**/secrets/
state/
*.state.json
.env.local

# Runtime state files (散在根目录, 跟 state/ 同类, 避免污染 git)
engine_stats.json
model_rules_state.json
penalty_state.json
public_keys_state.json

# Session notes (含 key audit 痕迹, 不推)
hot-cache/
secrets/
.env

# Backup files
*.bak-*
*.bak

# SMR 本地配置 (老大手动维护的 key 配置 + weekly_schedule, 不进 git)
config.yaml
config.yaml.bak.*
config.yaml.restore
